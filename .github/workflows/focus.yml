name: Update Focus Section

on:
  push:
    branches: [main]
    paths: ['README.md']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-focus:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Generate focus SVGs and update README
        run: python3 scripts/build_focus.py

      - name: Commit changes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git diff --quiet || (git add assets/focus/ README.md && git commit -m "chore: update focus section" && git push)
